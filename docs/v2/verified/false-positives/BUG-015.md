# BUG-015: `shaft.J` in `ElasticBearing`

| Field | Value |
|---|---|
| Verdict | false-positive |
| Review group | `initialization-conflict` |
| Original tier | Confirmed |
| Sanitizer result | `physical-bound-permits-zero` |
| Model | Modelica.Mechanics.Rotational.Examples.ElasticBearing |
| Target | `shaft.J` |
| Declaration/site | `Inertia.mo:4` |
| Original report | [BUG-elasticbearing-shaft-j.md](../../bugs/BUG-elasticbearing-shaft-j.md) |
| Original SHA-256 | `f98c38e90fcf053831a4d627759782efc7c20ea6936e7c095aabb9d67b908dc6` |

## Why this is not a verified bug

The nominal example runs. Recompiling with the reported zero removes a storage state and exposes inconsistent fixed initial equations, not an unavoidable reciprocal in the primitive component. Control Shaft keeps the zero and relaxes the relevant fixed initial conditions; it simulates successfully. The exact original trigger does fail, but its attribution to a generally invalid library min=0 is false. Fix the example initialization or constrain this particular example if it must retain those starts.

## Regression action

Correct the detector/adjudicator for this specific semantic case; do not impose a blanket strictly-positive bound on the library declaration.

## Evidence basis

Exact stable-ID source/execution review from the first exhaustive audit.

## Original claim

`Modelica.Mechanics.Rotational.Examples.ElasticBearing` runs cleanly at its declared values. Setting `shaft.J = 0` — a value the declaration permits — makes it fail, in Rumoca and in OpenModelica. The defect is in the **declaration**, not in this model. This model is the witness.

## Earlier independent review

[Prior report](../../../verifiedBugs/false-positives/BUG-015.md)

## Scope

Confirmed means independently reproduced execution evidence survived the semantic controls. Candidate means useful static/source evidence exists but a false positive is still possible. Advisory means the analyzer explicitly asks about unknown intent and makes no defect claim. False-positive means the stated defect claim is refuted; it does not certify every connected topology. Unresolved entries are deliberately not called real or fake.

[v2 verification index](../README.md)
