# Rumoca Bitcode: type reference

<!-- Generated by tools/bitcode/gen_reference.py. Do not edit: run
     `tools/bitcode/gen_reference.py` after changing schema.rs, and
     `--check` in CI fails when this file is stale. -->

The narrative — container, encodings, versioning, identity, provenance, validation — is in [SPEC_RUMOCA_BITCODE.md](SPEC_RUMOCA_BITCODE.md). This is the reference for what an artifact contains, derived from `crates/rumoca-bitcode/src/schema.rs` so that it cannot fall behind it.

**Compatibility** is per member. `additive` means a reader of an artifact written before the member existed still works, because the member is optional or defaulted; `required` means adding it is a version break.

56 types.

| Type | Kind | Members |
|---|---|---:|
| [`RbcModel`](#rbcmodel) | struct | 24 |
| [`RbcSummary`](#rbcsummary) | struct | 27 |
| [`RbcAction`](#rbcaction) | enum | 3 |
| [`RbcBinaryOp`](#rbcbinaryop) | enum | 13 |
| [`RbcBinder`](#rbcbinder) | struct | 5 |
| [`RbcBranch`](#rbcbranch) | struct | 2 |
| [`RbcCausality`](#rbccausality) | enum | 6 |
| [`RbcComponent`](#rbccomponent) | struct | 3 |
| [`RbcCondition`](#rbccondition) | struct | 3 |
| [`RbcConditionNode`](#rbcconditionnode) | enum | 10 |
| [`RbcConnection`](#rbcconnection) | struct | 8 |
| [`RbcConnectionSet`](#rbcconnectionset) | struct | 7 |
| [`RbcConnectorMember`](#rbcconnectormember) | struct | 2 |
| [`RbcCoordinate`](#rbccoordinate) | enum | 15 |
| [`RbcDiscreteActivation`](#rbcdiscreteactivation) | enum | 2 |
| [`RbcDiscreteBranch`](#rbcdiscretebranch) | struct | 3 |
| [`RbcDiscreteDefinition`](#rbcdiscretedefinition) | struct | 3 |
| [`RbcDiscreteRealActivation`](#rbcdiscreterealactivation) | enum | 2 |
| [`RbcDiscreteRealEquation`](#rbcdiscreterealequation) | struct | 7 |
| [`RbcDomain`](#rbcdomain) | struct | 6 |
| [`RbcEquation`](#rbcequation) | struct | 6 |
| [`RbcEquationFamily`](#rbcequationfamily) | struct | 10 |
| [`RbcEventAction`](#rbceventaction) | struct | 5 |
| [`RbcExpr`](#rbcexpr) | struct | 4 |
| [`RbcExprNode`](#rbcexprnode) | enum | 15 |
| [`RbcFile`](#rbcfile) | struct | 4 |
| [`RbcFlowBalance`](#rbcflowbalance) | struct | 2 |
| [`RbcFlowTerm`](#rbcflowterm) | struct | 2 |
| [`RbcFunction`](#rbcfunction) | struct | 7 |
| [`RbcFunctionBody`](#rbcfunctionbody) | enum | 2 |
| [`RbcFunctionParameter`](#rbcfunctionparameter) | struct | 2 |
| [`RbcGeneration`](#rbcgeneration) | enum | 25 |
| [`RbcInitialDiscreteValue`](#rbcinitialdiscretevalue) | struct | 3 |
| [`RbcInline`](#rbcinline) | enum | 3 |
| [`RbcLiteral`](#rbcliteral) | enum | 5 |
| [`RbcOrigin`](#rbcorigin) | enum | 2 |
| [`RbcProvenance`](#rbcprovenance) | struct | 2 |
| [`RbcQuantityKind`](#rbcquantitykind) | enum | 3 |
| [`RbcRecord`](#rbcrecord) | struct | 2 |
| [`RbcRecordField`](#rbcrecordfield) | struct | 2 |
| [`RbcRelation`](#rbcrelation) | struct | 3 |
| [`RbcRole`](#rbcrole) | enum | 8 |
| [`RbcRoot`](#rbcroot) | struct | 4 |
| [`RbcScalar`](#rbcscalar) | enum | 6 |
| [`RbcScalarView`](#rbcscalarview) | enum | 3 |
| [`RbcSchedule`](#rbcschedule) | enum | 2 |
| [`RbcSource`](#rbcsource) | struct | 3 |
| [`RbcSpan`](#rbcspan) | struct | 5 |
| [`RbcSubscript`](#rbcsubscript) | enum | 3 |
| [`RbcSymbolContract`](#rbcsymbolcontract) | struct | 9 |
| [`RbcTimeEvent`](#rbctimeevent) | struct | 3 |
| [`RbcTracePoint`](#rbctracepoint) | struct | 8 |
| [`RbcType`](#rbctype) | struct | 4 |
| [`RbcUnaryOp`](#rbcunaryop) | enum | 3 |
| [`RbcVariability`](#rbcvariability) | enum | 4 |
| [`RbcVariable`](#rbcvariable) | struct | 23 |

## RbcModel

A compiled Modelica model in public form.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `name` | `String` | required | Top-level model name as compiled, e.g. `"Circuit.Test"`. |
| `sources` | `Vec<RbcSource>` | required | Source files referenced by provenance in this artifact. |
| `types` | `Vec<RbcType>` | required | Value types referenced by variables and expressions. |
| `variables` | `Vec<RbcVariable>` | required | Every variable in the model, in every role. |
| `expressions` | `Vec<RbcExpr>` | required | Flat expression arena. Operands reference earlier entries by [`ExprId`]. |
| `equations` | `Vec<RbcEquation>` | required | Continuous residual equations: each asserts `residual == 0`. |
| `initial_equations` | `Vec<RbcEquation>` | required | Initialization residual equations. |
| `domains` | `Vec<RbcDomain>` | additive | Iteration domains referenced by the equation families. |
| `discrete_real_equations` | `Vec<RbcDiscreteRealEquation>` | additive | MLS Appendix B.1b coupled discrete-Real equations, which `equations` does not contain. |
| `initial_discrete_values` | `Vec<RbcInitialDiscreteValue>` | additive | Values discrete-valued variables take at the initialization instant. |
| `functions` | `Vec<RbcFunction>` | additive | Function declarations named by `RbcExprNode::Call`, without bodies. |
| `equation_families` | `Vec<RbcEquationFamily>` | additive | Array/`for` equations, which `equations` does not contain. |
| `initial_equation_families` | `Vec<RbcEquationFamily>` | additive | — |
| `relations` | `Vec<RbcRelation>` | required | Primitive comparisons that can generate events. |
| `conditions` | `Vec<RbcCondition>` | required | Boolean activation conditions over relations, clocks and discretes. |
| `roots` | `Vec<RbcRoot>` | required | Zero-crossing surfaces the solver must monitor. |
| `events` | `Vec<RbcEventAction>` | required | Actions performed when an event fires. |
| `time_events` | `Vec<RbcTimeEvent>` | required | Scheduled (time-triggered) events. |
| `connections` | `Vec<RbcConnection>` | required | Connector-level provenance recovered from flattening. |
| `connection_sets` | `Vec<RbcConnectionSet>` | additive | The connection graph's nodes: what each `connect` set equates and conserves. Empty where flattening did not record connections. |
| `components` | `Vec<RbcComponent>` | required | Component instances referenced by connections and variables. |
| `trace_points` | `Vec<RbcTracePoint>` | additive | Observation requests. Empty on export; a transformation pass adds these. |
| `discrete_definitions` | `Vec<RbcDiscreteDefinition>` | additive | MLS Appendix B.1c definitions: what each discrete-valued variable (Boolean, Integer, enumeration) is equal to, and under what activation. Separate from `equations`, which carries only continuous residuals, and from `events`, which carries reinit/assert/terminate actions. Without this a `discrete_value` variable is declared and never defined, and reconstruction rejects the artifact. |
| `summary` | `RbcSummary` | required | Counts a consumer can check against the collections above. Present so a truncated or partially-written artifact fails loudly. |

## RbcSummary

Denormalised counts, for cheap validation and for `bitcode inspect`.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `variables` | `u32` | required | — |
| `states` | `u32` | required | — |
| `parameters` | `u32` | required | — |
| `constants` | `u32` | required | — |
| `inputs` | `u32` | required | — |
| `outputs` | `u32` | required | — |
| `algebraics` | `u32` | required | — |
| `discrete_reals` | `u32` | required | — |
| `discrete_values` | `u32` | required | — |
| `equations` | `u32` | required | — |
| `initial_equations` | `u32` | required | — |
| `expressions` | `u32` | required | — |
| `relations` | `u32` | required | — |
| `conditions` | `u32` | required | — |
| `roots` | `u32` | required | — |
| `events` | `u32` | required | — |
| `time_events` | `u32` | required | — |
| `connections` | `u32` | required | — |
| `connection_sets` | `u32` | additive | Nodes of the connection graph. Defaulted so artifacts written before the exporter emitted sets still load. |
| `components` | `u32` | required | — |
| `trace_points` | `u32` | required | — |
| `discrete_definitions` | `u32` | additive | — |
| `discrete_real_equations` | `u32` | additive | MLS Appendix B.1b equations. Counted separately from `equations` because they determine a different partition of unknowns: adding them to the continuous count would make every model with a `sample` look over-constrained. |
| `initial_discrete_values` | `u32` | additive | — |
| `domains` | `u32` | additive | Families, and the scalar rows they stand for. Both are needed: a consumer counting equations wants the rows, one walking the artifact wants the families. |
| `equation_families` | `u32` | additive | — |
| `family_scalar_rows` | `u32` | additive | — |

## RbcAction

_No description._

| Variant | Meaning |
|---|---|
| `Reinitialize` | `reinit(state, value)`. |
| `Assert` | — |
| `Terminate` | — |

## RbcBinaryOp

_No description._

| Variant | Meaning |
|---|---|
| `Add` | — |
| `Subtract` | — |
| `Multiply` | — |
| `Divide` | — |
| `Power` | — |
| `Equal` | — |
| `NotEqual` | — |
| `Less` | — |
| `LessEqual` | — |
| `Greater` | — |
| `GreaterEqual` | — |
| `And` | — |
| `Or` | — |

## RbcBinder

One iteration binder: `for i in lower:step:upper`.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `u32` | required | — |
| `display_name` | `String` | required | — |
| `lower` | `i64` | required | — |
| `upper` | `i64` | required | — |
| `step` | `i64` | required | — |

## RbcBranch

One `condition -> value` arm of a conditional expression.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `condition` | `ExprId` | required | — |
| `value` | `ExprId` | required | — |

## RbcCausality

Interface causality, orthogonal to [`RbcRole`].

| Variant | Meaning |
|---|---|
| `Input` | — |
| `Output` | — |
| `Parameter` | — |
| `CalculatedParameter` | — |
| `Independent` | — |
| `Local` | — |

## RbcComponent

One component instance, identified by its flattened path prefix.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `ComponentId` | required | — |
| `path` | `String` | required | Dotted instance path, e.g. `"motor"` or `"drive.motor"`. Empty for the top-level model itself. |
| `class_name` | `Option<String>` | additive | Fully qualified class this instance is of, when flattening recorded it. Without this a consumer can see that a node joins `L.n` to `Ro.p` and cannot see that it joins an inductor to a resistor, which is most of what a connection graph is for. |

## RbcCondition

Boolean activation condition.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `ConditionId` | required | — |
| `node` | `RbcConditionNode` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcConditionNode

_No description._

| Variant | Meaning |
|---|---|
| `Initial` | True during initialization only. |
| `Always` | Always active; the section is not a `when` at all. |
| `Relation` | — |
| `Discrete` | — |
| `Not` | — |
| `And` | — |
| `Or` | — |
| `AnyRise` | MLS §8.3.5 vector activation: fires when any element rises. |
| `Clock` | Clocked activation this schema version does not detail. |
| `Unsupported` | — |

## RbcConnection

One `connect(...)` relationship recovered from flattening. A connection is an equality between two connector endpoints, plus the conservation law over the flow members of the connection set. It is **not** a directional message: `left` and `right` are symmetric.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `ConnectionId` | required | — |
| `left` | `VariableId` | required | Endpoint variable on one side. |
| `right` | `VariableId` | required | Endpoint variable on the other side. |
| `quantity` | `RbcQuantityKind` | required | Physical role of the quantity being connected. |
| `left_connector` | `String` | required | Connector instance paths, e.g. `("battery.pin", "motor.pin")`. |
| `right_connector` | `String` | required | — |
| `equation` | `Option<EquationId>` | additive | Equation this connection produced, when one was generated. |
| `provenance` | `RbcProvenance` | required | — |

## RbcConnectionSet

One node of the connection graph: everything `connect(...)` joined together. A `connect` is written pairwise, but the object it creates is n-ary. Three pins wired to one node share **one** potential and **one** conservation law, and the flow balance over them is not expressible as three pairs. MLS §9.2 calls this the connection set, and it is the unit every question worth asking about a network is asked about: what is equated here, what is conserved here, and which components meet here.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `ConnectionSetId` | required | — |
| `connectors` | `Vec<String>` | required | Connector instances joined at this node, e.g. `["L.n", "Ro.p"]`. |
| `potentials` | `Vec<VariableId>` | required | Potential members, all equal at this node. |
| `balances` | `Vec<RbcFlowBalance>` | additive | The conservation laws this node asserts, one per flow member kind. Plural because a connector may declare several flow members: a MultiBody frame conserves a force *and* a torque at the same node. They are separate sums and must stay separate --- a check that treated them as one reported a force and a torque as a unit mismatch, which is the node's structure being wrong rather than the model's. |
| `potential_equations` | `Vec<EquationId>` | additive | The DAE equations carrying the potential equalities of this node. |
| `unconnected` | `bool` | additive | A connector nothing was connected to, whose flow MLS §9.2 sets to zero. Not a defect on its own --- a model compiled standalone has unconnected ports by construction --- but the distinction between "conserved among several" and "forced to zero alone" is one a reader needs, and it is invisible once both are just equations. |
| `provenance` | `RbcProvenance` | required | — |

## RbcConnectorMember

Modelica connector semantics for one variable. A `flow` member obeys a sum-to-zero conservation law; a `potential` member is equated across a connection. Preserving this distinction is why connections are not modelled as directional messages.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `quantity` | `RbcQuantityKind` | required | — |
| `connected` | `bool` | required | True when this member participates in at least one `connect(...)`. |

## RbcCoordinate

A leaf referencing a model quantity. This is how expressions name variables — never by string.

| Variant | Meaning |
|---|---|
| `Parameter` | — |
| `Input` | — |
| `State` | — |
| `Derivative` | `der(x)` for state `variable`. |
| `Algebraic` | — |
| `DiscreteReal` | — |
| `DiscreteValue` | — |
| `PreState` | — |
| `PreAlgebraic` | — |
| `PreDiscreteReal` | — |
| `PreDiscreteValue` | — |
| `Time` | Simulation time. |
| `Binder` | A family's iteration binder — the `i` in `x[i] = i`. Without this a family body cannot be expressed, so exporting families without it produced artifacts that failed their own import. |
| `Condition` | A condition's value used inside an expression, such as `initial()`. |
| `FunctionParameter` | A function's formal parameter, read from inside that function's body. |

## RbcDiscreteActivation

_No description._

| Variant | Meaning |
|---|---|
| `Always` | A plain equation, active whenever the model is: `b = x < 0.5`. |
| `When` | A `when` branch, active on its trigger under its guard. |

## RbcDiscreteBranch

One activation branch of a B.1c definition. `values` has exactly one entry per target of the owning definition, in the same order.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `activation` | `RbcDiscreteActivation` | required | — |
| `values` | `Vec<ExprId>` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcDiscreteDefinition

One MLS Appendix B.1c definition owner: a set of discrete-valued targets defined together, and the branches that give them values.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `targets` | `Vec<VariableId>` | required | The discrete-valued variables this owner defines, in the order the branches' `values` follow. |
| `branches` | `Vec<RbcDiscreteBranch>` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcDiscreteRealActivation

When a B.1b equation holds.

| Variant | Meaning |
|---|---|
| `Always` | Holds continuously, like a B.1a residual. |
| `When` | Holds while `guard` is active, re-evaluated when `trigger` rises. |

## RbcDiscreteRealEquation

An MLS Appendix B.1b coupled discrete-Real equation. These determine the discrete-Real variables — a `sample`d hold, a mean held between events — and live in their own DAE partition, which nothing exported. An artifact was therefore short by exactly one equation per discrete-Real variable, and a consumer counting equations saw a model with more constraints than unknowns: 19 phantom `UNMATCHED_EQUATION`s on `Polyphase.Examples.Utilities.AnalysatorAC` alone.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `EquationId` | required | — |
| `residual` | `ExprId` | required | — |
| `activation` | `RbcDiscreteRealActivation` | required | — |
| `reads` | `Vec<VariableId>` | additive | — |
| `reads_derivative` | `Vec<VariableId>` | additive | — |
| `reads_previous` | `Vec<VariableId>` | additive | Variables whose *left limit* `pre(v)` this equation reads (MLS §3.7.5). Kept apart from `reads`, because the two answer different questions. A dependency analysis wants them together: the equation does depend on `v`. A *matching* analysis must not see them at all — `pre(v)` is the value `v` held at event entry, a known, so an equation reading it is not a candidate to determine `v`. Merged into `reads`, the B.1b equation `y_last = f(pre(x))` looked able to determine the state `x`, the matching spent it there, and `y_last` came out unmatched. |
| `provenance` | `RbcProvenance` | required | — |

## RbcDomain

The iteration domain an equation family ranges over. Carried so a family can be *rebuilt*, not merely counted. Without it, import had to refuse any artifact containing a family, because a DAE reconstructed without the domain would be missing those equations and would still validate.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `DomainId` | required | — |
| `binders` | `Vec<RbcBinder>` | required | — |
| `parent` | `Option<DomainId>` | additive | — |
| `extents` | `Vec<u32>` | required | — |
| `scalar_count` | `u32` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcEquation

A residual equation: the model asserts `residual == 0`.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `EquationId` | required | — |
| `residual` | `ExprId` | required | — |
| `provenance` | `RbcProvenance` | required | — |
| `reads` | `Vec<VariableId>` | additive | Variables this equation reads, precomputed by the compiler's own dependency projection. A consumer must not have to re-derive these by walking expressions. |
| `reads_derivative` | `Vec<VariableId>` | additive | States whose derivative this equation reads, i.e. `der(x)` occurrences. |
| `reads_previous` | `Vec<VariableId>` | additive | Variables whose *left limit* `pre(v)` this equation reads (MLS §3.7.5). Kept apart from `reads`, because the two answer different questions. A dependency analysis wants them together: the equation does depend on `v`. A *matching* analysis must not see them at all — `pre(v)` is the value `v` held at event entry, a known, so an equation reading it is not a candidate to determine `v`. Merged into `reads`, the B.1b equation `y_last = f(pre(x))` looked able to determine the state `x`, the matching spent it there, and `y_last` came out unmatched. |

## RbcEquationFamily

An array or `for` equation, kept in the compact form the DAE holds. Expanding it to scalar rows at export would multiply the artifact by the array extent and lose the fact that the rows share one source equation. The alternative that was in place — omitting families entirely — is worse: the artifact then describes fewer equations than the model has, validates cleanly, and any consumer reasoning about solvability reads an incomplete system with no way to detect it. See TOOLBUG-014.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `FamilyId` | required | — |
| `domain` | `DomainId` | required | The domain this family ranges over. |
| `bodies` | `Vec<ExprId>` | required | Residual bodies, one per equation in the family's body list. |
| `scalar_rows` | `u32` | required | How many scalar equations this stands for. This is the number a balance or matching analysis must count. |
| `extents` | `Vec<u32>` | required | Extent of each iteration axis, outermost first. |
| `scalar_view` | `RbcScalarView` | required | — |
| `reads` | `Vec<VariableId>` | additive | Variables the family's rows read, from the compiler's own scalar coordinate projection — the same authority `RbcEquation.reads` uses. Walking the symbolic body instead misses array and record selections, which is a missing incidence edge, which is a matching failure a consumer would report as a defect in the model. |
| `reads_derivative` | `Vec<VariableId>` | additive | — |
| `reads_previous` | `Vec<VariableId>` | additive | Variables whose *left limit* `pre(v)` this equation reads (MLS §3.7.5). Kept apart from `reads`, because the two answer different questions. A dependency analysis wants them together: the equation does depend on `v`. A *matching* analysis must not see them at all — `pre(v)` is the value `v` held at event entry, a known, so an equation reading it is not a candidate to determine `v`. Merged into `reads`, the B.1b equation `y_last = f(pre(x))` looked able to determine the state `x`, the matching spent it there, and `y_last` came out unmatched. |
| `provenance` | `RbcProvenance` | required | — |

## RbcEventAction

Something the model does when an event fires.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `EventId` | required | — |
| `trigger` | `ConditionId` | required | — |
| `guard` | `ConditionId` | required | — |
| `action` | `RbcAction` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcExpr

_No description._

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `ExprId` | required | — |
| `value_type` | `TypeId` | required | — |
| `node` | `RbcExprNode` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcExprNode

Expression node. Operands always reference nodes with a **lower** [`ExprId`], so the arena is a DAG in topological order and can be evaluated in one pass.

| Variant | Meaning |
|---|---|
| `Literal` | — |
| `Coordinate` | — |
| `Unary` | — |
| `Binary` | — |
| `Conditional` | `if c1 then v1 elseif c2 then v2 else fallback`. Branches are evaluated in order; `fallback` is required, so the node is total. |
| `Builtin` | A pure built-in call: `sqrt(x)`, `log(x)`, `abs(x)`, `min(a, b)`, ... `name` is the Modelica spelling in lower case, so a consumer matches on `"sqrt"` rather than on an enum ordinal that could shift. |
| `Array` | An array construction `{a, b, c}`. An empty array carries its element type, which cannot be inferred from zero elements. |
| `Record` | A record construction, one operand per field in declaration order. |
| `Field` | `base.field`, by field ordinal in the record's declaration order. |
| `Range` | `start:step:stop`. The bounds are Integer literals by construction. |
| `Comprehension` | `{body for i in ...}` over an iteration domain. |
| `Index` | `base[s1, s2, ...]`. |
| `ArrayUpdate` | A functional array update: `base` with `subscripts` replaced by `value`. |
| `Call` | One result projection of a function call. `owner` is the first projection of this call occurrence, and equals the node's own id for a single-output call. Two projections of one call share it, so a consumer knows `(a, b) = f(x)` is one evaluation of `f` and not two. |
| `Unsupported` | A node this schema version cannot represent. A consumer must treat the containing model as not fully understood rather than assume a default. Producers only emit this when explicitly asked to tolerate gaps. |

## RbcFile

One `.rbc` artifact.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `magic` | `String` | required | Always [`RBC_MAGIC`]. |
| `bitcode_version` | `u32` | required | Public contract version. See [`RBC_VERSION`]. |
| `producer` | `String` | required | Free-form producer identification, e.g. `"rumoca 0.10.0"`. Informational only: a consumer must not change behaviour based on it. |
| `model` | `RbcModel` | required | The compiled model. |

## RbcFlowBalance

One conservation law at a node: a signed sum of flow members, equal to zero.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `equation` | `Option<EquationId>` | additive | The DAE equation carrying it, when it could be paired. |
| `terms` | `Vec<RbcFlowTerm>` | required | — |

## RbcFlowTerm

One flow member of a connection set, with the sign the balance gives it.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `variable` | `VariableId` | required | — |
| `negated` | `bool` | additive | Whether the balance subtracts this term. Outflow from one component is inflow to another, and the sign is what makes the sum mean anything. |

## RbcFunction

One function declaration, without its body. The signature is what a *call site* needs: which function, how many arguments, what they mean. The body is a separate IR — SSA definitions, loop transitions, conditionals, external interfaces — and bitcode v1 does not carry it, which `body` records explicitly so an absent body is never mistaken for an empty one.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `FunctionId` | required | — |
| `name` | `String` | required | Fully qualified Modelica name, e.g. `"Modelica.Math.asin"`. |
| `parameters` | `Vec<RbcFunctionParameter>` | required | — |
| `results` | `Vec<TypeId>` | required | Result types in declaration order; a call names one by ordinal. |
| `inline` | `RbcInline` | required | The MLS §18.3 `Inline`/`LateInline` request the declaration wrote. |
| `body` | `RbcFunctionBody` | required | — |
| `declaration` | `RbcProvenance` | required | — |

## RbcFunctionBody

What kind of body the declaration has, and whether this artifact carries it.

| Variant | Meaning |
|---|---|
| `ElidedModelica` | A Modelica body exists but is not in this artifact. |
| `External` | An MLS §12.9 external body, named by language and symbol. Carried because it is the whole of what the function does: there is no Modelica body that could be elided. |

## RbcFunctionParameter

_No description._

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `name` | `String` | required | — |
| `value_type` | `TypeId` | required | — |

## RbcGeneration

Lowering kinds a consumer may care about. Mirrors the compiler's own classification; `Other` keeps a v1 reader working against a producer that learns a new kind.

| Variant | Meaning |
|---|---|
| `SyntheticResidual` | — |
| `BindingEquation` | — |
| `ConnectionEquation` | — |
| `FlowBalanceEquation` | — |
| `AlgorithmEquation` | — |
| `DiscreteUpdate` | — |
| `ConditionLowering` | — |
| `PreValueLowering` | — |
| `ClockLowering` | — |
| `DelayLowering` | — |
| `SemiLinearLowering` | — |
| `TerminalLowering` | — |
| `EventActionLowering` | — |
| `InitializationEquation` | — |
| `DefaultStart` | — |
| `ArrayEquationProjection` | — |
| `RecordEquationProjection` | — |
| `FunctionLoopLowering` | — |
| `FunctionConditionLowering` | — |
| `FunctionAggregateLowering` | — |
| `DerivedParameterLowering` | — |
| `IndexReduction` | — |
| `AliasElimination` | — |
| `RuntimeDiscontinuity` | — |
| `Other` | A lowering kind this schema version does not name. |

## RbcInitialDiscreteValue

One discrete-valued variable's value at the initialization instant.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `target` | `VariableId` | required | — |
| `value` | `ExprId` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcInline

_No description._

| Variant | Meaning |
|---|---|
| `Unstated` | — |
| `Requested` | — |
| `Never` | — |

## RbcLiteral

_No description._

| Variant | Meaning |
|---|---|
| `Real` | — |
| `Integer` | — |
| `Enumeration` | An enumeration value, carried as its 1-based ordinal (MLS §4.9.5). This is distinct from `Integer` even though both hold an integer: the DAE's type checker demands an `Enumeration` where the declaration says enumeration, and rebuilding one from an `Integer` literal is rejected with `expected Enumeration, found Integer`. |
| `Boolean` | — |
| `String` | — |

## RbcOrigin

Whether an object was written by the author or produced by lowering. `Generated` carries the lowering kind so a consumer can distinguish a connection equation from a flow balance without guessing from shape.

| Variant | Meaning |
|---|---|
| `Source` | Written in the source at `span`. |
| `Generated` | Produced by the compiler; `span` names the nearest responsible source. |

## RbcProvenance

Why a model object exists and where it came from.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `origin` | `RbcOrigin` | required | — |
| `span` | `RbcSpan` | required | — |

## RbcQuantityKind

_No description._

| Variant | Meaning |
|---|---|
| `Potential` | Equated across a connection (voltage, angle, temperature). |
| `Flow` | Conserved across a connection; signed sum is zero (current, torque). |
| `Stream` | Stream variable (MLS §15). |

## RbcRecord

A record type's name and fields, in declaration order. A field's type is a `TypeId` into the same table, and always precedes the record that names it, so one forward pass rebuilds the table.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `name` | `String` | required | — |
| `fields` | `Vec<RbcRecordField>` | required | — |

## RbcRecordField

_No description._

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `name` | `String` | required | — |
| `value_type` | `TypeId` | required | — |

## RbcRelation

A primitive comparison that can generate an event.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `RelationId` | required | — |
| `expression` | `ExprId` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcRole

Appendix-B partition a variable belongs to.

| Variant | Meaning |
|---|---|
| `Parameter` | — |
| `Constant` | — |
| `Input` | — |
| `State` | — |
| `Algebraic` | — |
| `Output` | — |
| `DiscreteReal` | — |
| `DiscreteValue` | — |

## RbcRoot

A zero-crossing surface the solver monitors.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `RootId` | required | — |
| `relation` | `RelationId` | required | — |
| `activation` | `ConditionId` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcScalar

_No description._

| Variant | Meaning |
|---|---|
| `Real` | — |
| `Integer` | — |
| `Boolean` | — |
| `String` | — |
| `Enumeration` | — |
| `Record` | — |

## RbcScalarView

How a family's symbolic body becomes one scalar row (MLS §8.3.2, §11.2.2).

| Variant | Meaning |
|---|---|
| `BinderSubstitution` | Substitute the binder values into the symbolic body. |
| `RowMajorProjection` | Select the row-major element from an aggregate body. |
| `BinderPrefixProjection` | Substitute leading binders, project the remaining axes. |

## RbcSchedule

_No description._

| Variant | Meaning |
|---|---|
| `Static` | Fires at a fixed instant, as an exact rational `numerator/denominator`. |
| `Dynamic` | Fires at a time computed by an expression. |

## RbcSource

One source file.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `SourceId` | required | — |
| `name` | `String` | required | Path or logical name as Rumoca saw it. |
| `text` | `Option<String>` | additive | Full source text, when the producer was asked to embed it. Absent keeps artifacts small; present makes them self-contained. |

## RbcSpan

A resolved source location. Byte offsets are authoritative; line and column are precomputed at export time so consumers never need the source text to report `Model.mo:52`.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `source` | `SourceId` | required | — |
| `start` | `u32` | required | — |
| `end` | `u32` | required | — |
| `line` | `u32` | required | 1-based line of `start`. |
| `column` | `u32` | required | 1-based column of `start`. |

## RbcSubscript

One subscript position of an index or array-update node. `Index` selects a single element and drops the dimension; `Slice` selects several and keeps it; `Whole` is a bare `:`.

| Variant | Meaning |
|---|---|
| `Index` | — |
| `Whole` | — |
| `Slice` | — |

## RbcSymbolContract

What a declaration promises about a symbol. Carried per symbol and preserved through flattening, so a consumer need not re-derive it from names or guess it from the DAE partition. Each field answers a question that the others do not: * `variability` — may the value change, and when. * `is_final` — may a *modifier* override this declaration. Says nothing about whether the binding depends on something adjustable. * `is_protected` — who may see it. Visibility is not immutability, and a `protected parameter` is still settable before translation. * `evaluate` — the MLS §18.3 hint that a value may be substituted at translation time. A hint, not a guarantee, and not equivalent to `constant`. * `effective_value` — the binding's value when it is statically evaluable. * `binding_depends_on` — the symbols the binding reads, so a consumer can walk the chain rather than trusting a leaf.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `variability` | `RbcVariability` | required | — |
| `is_final` | `bool` | additive | — |
| `is_protected` | `bool` | additive | — |
| `evaluate` | `bool` | additive | `annotation(Evaluate=true)`, or a `final` declaration the compiler treats the same way for structural evaluation. |
| `structural` | `bool` | additive | MLS §18.3: changing this requires retranslation, not just a restart. |
| `effective_value` | `Option<f64>` | additive | The binding's value where the compiler could evaluate it statically. |
| `binding_depends_on` | `Vec<VariableId>` | additive | Symbols the binding reads. Empty for a literal binding or none at all. |
| `binding_from_modification` | `bool` | additive | Whether the binding came from a modifier rather than the declaration. |
| `declared_in` | `Option<String>` | additive | The class that declared this symbol, and the declaration's own span. |

## RbcTimeEvent

A scheduled event.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `EventId` | required | — |
| `schedule` | `RbcSchedule` | required | — |
| `provenance` | `RbcProvenance` | required | — |

## RbcTracePoint

A request to observe a value at runtime. Trace points are **observation metadata**, deliberately separate from the physical equations. Adding one does not change model semantics, which is why an instrumentation pass does not need to rewrite equations.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `TracePointId` | required | — |
| `variable` | `VariableId` | required | The variable to observe. |
| `label` | `String` | required | Human-readable label for the trace output. |
| `connection` | `Option<ConnectionId>` | additive | Connection this observation belongs to, when it came from connector instrumentation. |
| `connection_set` | `Option<ConnectionSetId>` | additive | Connection *set* --- the node --- this observation belongs to. Separate from `connection` because they answer different questions: a pairwise connect is an edge, and the node is where a conservation law lives. An observation of a flow member belongs to the node, and recording it against one of the edges that happen to form that node would name an arbitrary one of them. |
| `quantity` | `Option<RbcQuantityKind>` | additive | Physical role of the observed quantity, when known. |
| `unit` | `Option<String>` | additive | — |
| `added_by` | `Option<String>` | additive | Tool that added this trace point, for provenance of the instrumentation itself. |

## RbcType

A value type: a scalar, or a rectangular array of one.

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `TypeId` | required | — |
| `scalar` | `RbcScalar` | required | — |
| `dimensions` | `Vec<u32>` | additive | Empty for a scalar; otherwise the array extents in row-major order. |
| `record` | `Option<RbcRecord>` | additive | Set when `scalar` is `record`. Without it a record type flattened to a bare scalar tag, and a `Record` or `Field` node rebuilt against it was rejected for having the wrong field count. |

## RbcUnaryOp

_No description._

| Variant | Meaning |
|---|---|
| `Negate` | — |
| `Not` | — |
| `Plus` | MLS §3.4 unary plus, the identity. Carried rather than dropped because it reaches the DAE intact whenever constant folding does not consume it — `parameter SI.Voltage Vps=+15` under `--no-fold-parameter-bindings`. Encoding it as its operand is not possible here: expressions are addressed by index and each node must occupy its own, so the alternative to a variant is failing the export. |

## RbcVariability

MLS §3.8 variability, as the declaration wrote it. Distinct from [`RbcRole`], which is the Appendix-B partition the DAE placed the coordinate in. A `constant` and a `parameter` are both role `parameter`; only variability tells them apart.

| Variant | Meaning |
|---|---|
| `Constant` | — |
| `Parameter` | — |
| `Discrete` | — |
| `Continuous` | — |

## RbcVariable

_No description._

| Member | Type | Compatibility | Meaning |
|---|---|---|---|
| `id` | `VariableId` | required | — |
| `name` | `String` | required | Fully-qualified flattened name, e.g. `"motor.flange.tau"`. |
| `role` | `RbcRole` | required | — |
| `causality` | `RbcCausality` | required | — |
| `value_type` | `TypeId` | required | — |
| `scalar_count` | `u32` | required | Number of scalars this variable expands to (1 for a scalar). |
| `contract` | `Option<RbcSymbolContract>` | additive | What the source says this symbol *is*: whether it may change, who may change it, and what its value depends on. Without this an analysis has only `role`, which answers a different question. `role` is the Appendix-B partition a coordinate belongs to; the contract is the declaration's promise. A divide-by-zero analysis that reads `role` alone proposes setting `Modelica.Constants.pi` to zero, and did. |
| `discrete_input` | `bool` | additive | Set on an `input` whose variability is discrete rather than continuous. The role alone does not determine it: a `Boolean` input is discrete and a `Real` input is usually, but not always, continuous. Import assumed continuous for every input, so a model with a `Boolean` input exported cleanly and was then rejected by its own importer. |
| `declaration` | `RbcProvenance` | required | — |
| `component` | `Option<ComponentId>` | additive | Component instance this variable belongs to, when it is inside one. |
| `unit` | `Option<String>` | additive | — |
| `physical_quantity` | `Option<String>` | additive | The MLS §4.8 `quantity` attribute the declaration carries — `"Mass"`, `"Resistance"`, `"ThermodynamicTemperature"`. This is the *semantic* identity of what the variable measures, which a unit alone does not give: `"Ohm"` says nothing about whether a value must be positive, while `quantity="Resistance"` on a passive component does. An external physical-invariant checker needs this to match rules to variables without inferring meaning from their names. |
| `declaring_class` | `Option<String>` | additive | Fully qualified Modelica class that declared this variable, e.g. `"Modelica.Electrical.Analog.Basic.Resistor"`. `physical_quantity` says *what a variable measures*; this says *what declared it*, and the two answer different questions. `quantity = "Resistance"` holds equally for a passive resistor and for a negative-impedance converter, so a rule that must apply to only one of them has to match on the declaring class. Flattening reduces a component to a path prefix on a name, so no consumer can recover this afterwards. |
| `description` | `Option<String>` | additive | — |
| `binding` | `Option<ExprId>` | additive | Declaration binding expression, when the source gave one. |
| `start` | `Option<ExprId>` | additive | — |
| `min` | `Option<ExprId>` | additive | — |
| `max` | `Option<ExprId>` | additive | — |
| `nominal` | `Option<ExprId>` | additive | — |
| `fixed` | `Option<bool>` | additive | — |
| `tunable` | `bool` | additive | — |
| `from_source` | `bool` | additive | True when the variable was written in the source rather than generated. |
| `connector` | `Option<RbcConnectorMember>` | additive | Connector semantics recovered from flattening, when this variable is a connector member. |

