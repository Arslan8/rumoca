# BUG-003: Zero-storage example has incompatible fixed initial conditions

| Field | Value |
|---|---|
| Verdict | false-positives |
| Scope / group | initialization-conflict |
| Model | Modelica.Mechanics.Translational.Examples.Damper |
| Target | mass2.m |
| Student classification | physical-bound-permits-zero |
| Original report | [BUG-damper-mass2-m.md](../../v2/bugs/BUG-damper-mass2-m.md) — reviewed as `BUG-003-damper-mass2-m.md`, which a later run renamed |
| Original SHA-256 | 7e9f312ee9ed327f3c009b8c21d68e022e07c33db4ea0340ca6019d70e0ab8ef |

## Why this is a false positive

The nominal example runs. Recompiling with the reported zero removes a storage state and exposes inconsistent fixed initial equations, not an unavoidable reciprocal in the primitive component. Control Mass2 keeps the zero and relaxes the relevant fixed initial conditions; it simulates successfully. The exact original trigger does fail, but its attribution to a generally invalid library min=0 is false. Fix the example initialization or constrain this particular example if it must retain those starts.

## Source evidence

Compiler/source-resolved declaration: `Mechanics/Translational/Components/Mass.mo:3`. Role: `parameter`; binding: `1`; effective min: `0`; effective max: `None`. 

[Mechanics/Translational/Components/Mass.mo — source snapshot](../evidence/sources/d34580bdea973245-Mass.mo)

```modelica
1: within Modelica.Mechanics.Translational.Components;
2: model Mass "Sliding mass with inertia"
3:   parameter SI.Mass m(min=0, start=1) "Mass of the sliding mass";
4:   parameter StateSelect stateSelect=StateSelect.default
5:     "Priority to use s and v as states" annotation (Dialog(tab="Advanced"));
6:   extends Translational.Interfaces.PartialRigid(L=0,s(start=0, stateSelect=
```


## Execution evidence

[Rumoca commands, return codes, integrity hashes, bounded output and metadata](../evidence/2f5c9ac506c1b046.json). Baseline: **clean**.

| Probe | Outcome |
|---|---|
| `mass2.m=0` | reported-failure |

Diagnostic: solve-IR evaluation failed: algebraic projection did not converge at event boundary: worst scaled residual row=16 target=mass2.a value=2.500000e2 ratio=2.500000e12 norm=2.500000e2 row_scale=1.000000e0 scaled_tolerance=1.000000e-10

[Independent OpenModelica wrappers and complete output](../evidence/omc-2f5c9ac506c1b046.json). Baseline: simulation succeeded.

- `mass2.m=0` set before translation: LOG_ASSERT        | debug   | division by zero at time 0, (a=-250) / (b=0), where divisor b expression is: mass2.m.
- Same value declared final, with final-parameter evaluation: LOG_INIT          | error   | The initialization problem is inconsistent due to the following equation: 0 != 250 = $START.mass2.s - mass2.s.

[Independent controls and actual library-record projections](../evidence/control-initialization.json) include source wrappers, compiler options, and full results.

## Recommended action

Correct the detector/adjudicator for this specific semantic case; do not impose a blanket strictly-positive bound on the library declaration.

## Scope and limitations

A source-level verdict addresses the reported declaration/claim, not every possible connected system. Tests cover 0–0.5 seconds and are not a proof of long-run stability. Shared instances are not distinct root causes. A missing bound, timeout, compiler error or failed nominal run alone never counts as a verified bug or a false positive. Fixes are proposals; no library/compiler implementation was changed.

[Group and related reports](../groups/initialization-conflict.md) · [Index](../README.md)
