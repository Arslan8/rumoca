# BUG-009: Zero-storage example has incompatible fixed initial conditions

| Field | Value |
|---|---|
| Verdict | false-positives |
| Scope / group | initialization-conflict |
| Model | Modelica.Electrical.Analog.Examples.ParallelResonance |
| Target | capacitor2.C |
| Student classification | physical-bound-permits-zero |
| Original report | [BUG-parallelresonance-capacitor2-c.md](../../v2/bugs/BUG-parallelresonance-capacitor2-c.md) — reviewed as `BUG-009-parallelresonance-capacitor2-c.md`, which a later run renamed |
| Original SHA-256 | d81ee66a88af4af2f74db7f77733165fca1561262aebacaa1ff9616ebea924b3 |

## Why this is a false positive

The nominal example runs. Recompiling with the reported zero removes a storage state and exposes inconsistent fixed initial equations, not an unavoidable reciprocal in the primitive component. Control ParallelC2 keeps the zero and relaxes the relevant fixed initial conditions; it simulates successfully. The exact original trigger does fail, but its attribution to a generally invalid library min=0 is false. Fix the example initialization or constrain this particular example if it must retain those starts.

## Source evidence

Compiler/source-resolved declaration: `Electrical/Analog/Basic/Capacitor.mo:4`. Role: `parameter`; binding: `(0.001 / (2 * (2 * asin(1.0))))`; effective min: `0`; effective max: `None`. 

[Electrical/Analog/Basic/Capacitor.mo — source snapshot](../evidence/sources/441fb6d5cbd9ea34-Capacitor.mo)

```modelica
2: model Capacitor "Ideal linear electrical capacitor"
3:   extends Interfaces.OnePort(v(start=0));
4:   parameter SI.Capacitance C(start=1) "Capacitance";
5: 
6: equation
7:   i = C*der(v);
```


## Execution evidence

[Rumoca commands, return codes, integrity hashes, bounded output and metadata](../evidence/0cc25a901687c470.json). Baseline: **clean**.

| Probe | Outcome |
|---|---|
| `capacitor2.C=0` | reported-failure |

Diagnostic: solve-IR evaluation failed: non-finite derivative evaluation for state 'sineCurrent.n.i'

[Independent OpenModelica wrappers and complete output](../evidence/omc-0cc25a901687c470.json). Baseline: simulation succeeded.

- `capacitor2.C=0` set before translation: LOG_ASSERT        | debug   | division by zero at time 0, (a=1) / (b=0), where divisor b expression is: capacitor2.C.
- Same value declared final, with final-parameter evaluation: LOG_INIT          | error   | The initialization problem is inconsistent due to the following equation: 0 != -1.5708 = $START.cosineCurrent.phi - cosineCurrent.phi.

[Independent controls and actual library-record projections](../evidence/control-initialization.json) include source wrappers, compiler options, and full results.

## Recommended action

Correct the detector/adjudicator for this specific semantic case; do not impose a blanket strictly-positive bound on the library declaration.

## Scope and limitations

A source-level verdict addresses the reported declaration/claim, not every possible connected system. Tests cover 0–0.5 seconds and are not a proof of long-run stability. Shared instances are not distinct root causes. A missing bound, timeout, compiler error or failed nominal run alone never counts as a verified bug or a false positive. Fixes are proposals; no library/compiler implementation was changed.

[Group and related reports](../groups/initialization-conflict.md) · [Index](../README.md)
